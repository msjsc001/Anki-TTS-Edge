# 核心依赖
edge-tts>=7.0.0
customtkinter>=5.2.2
playsound>=1.3.0 # Note: May have issues on some systems, consider alternatives if needed
pyperclip>=1.9.0
pystray>=0.19.0
Pillow>=9.0.0 # pystray dependency for icons
pygame>=2.1.0 # For audio playback
pynput>=1.7.0 # For global mouse/keyboard listeners (optional features)
pywin32>=300 # For Windows-specific clipboard/tray features

# 开发依赖（可选）
pytest>=7.2.0
black>=22.10.0
